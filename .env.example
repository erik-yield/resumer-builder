# Optional — only needed for local dev with the old Express server (no longer used).
# For Vercel, add your API key in the app Settings UI instead.

OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.0-flash-001
